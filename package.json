{
  "name": "stockar-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend estático servido desde /public",
  "scripts": {
    "live": "live-server public --port=5500 --host=127.0.0.1 --open=Login.html --watch=public --wait=200",
    "preview": "http-server public -p 5500 -a 127.0.0.1 -c-1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "http-server": "^14.1.1",
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}


